import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartincomeplanner.app',
  appName: 'Smart Income Planner',
  webDir: 'dist'
};

export default config;
